# HigherEdJobs

**Status: DEPRECATED — HTML structure changed.**

Reason: The endpoint now returns HTML instead of the expected RSS feed. The RSS parser receives no `<item>` elements, producing zero jobs.

The scraper remains in the codebase but returns empty results until the endpoint is updated or the HTML structure is re-parsed.
