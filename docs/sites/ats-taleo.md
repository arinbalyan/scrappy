# Taleo

**Status: DEPRECATED — public API removed / auth-gated.**

Reason: DNS resolution failure — `*.taleo.net` no longer resolves. Oracle has migrated Taleo to OCI infrastructure.

The scraper remains in the codebase but will return empty results
until a new API endpoint or authentication method is identified.
