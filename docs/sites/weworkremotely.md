# WeWorkRemotely

**Status: DEPRECATED — HTML structure changed.**

Reason: Site returns HTTP 406 Not Acceptable. The server rejects the request headers sent by the scraper, so the response contains no job data to parse.

The scraper remains in the codebase but returns empty results until the request header negotiation is fixed.
