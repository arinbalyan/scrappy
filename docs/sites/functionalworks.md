# FunctionalWorks

**Status: DEPRECATED — HTML structure changed.**

Reason: FunctionalWorks returns HTTP 418 ("teapot") — the GraphQL query sent by the scraper is no longer accepted by the server. The API endpoint or query format has changed.

The scraper remains in the codebase but returns empty results until the GraphQL query is updated.
